name: PR-Pipeline
on:
  pull_request:

  workflow_dispatch:

jobs:
  pr-pipline:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: install dependencies
        uses: ./.github/actions/pnpm-install@feature/GHA-PRpipeline

      - name: build env
        shell: bash
        run: pnpm dev





